KAAM GRADIENT APP ICON PACKAGE

The m silhouette and two-dot placement were extracted from the supplied KAAM logo. The letter is normalized to white and the dots to #FF5BA1.

PNG/ contains rounded-square master exports.
iOS/ contains an opaque 1024px full-bleed App Store icon (no baked-in corner mask).
Android/ contains a safe-area preview plus separate foreground/background layers.
Transparent/ contains the standalone symbol on alpha.
WEBP, SVG and PDF contain the 4096px master presentation.

import type { Metadata } from "next";
import { Footer } from "@/components/layout/footer"; import { Header } from "@/components/layout/header"; import { MarketingShell, PageHero } from "@/components/marketing/page-hero";
export const metadata: Metadata = { title: "How it works", description: "Understand KAAM's skills-first mutual-interest matching process." };
export default function HowItWorksPage(){const steps=[["1. Build the picture","Candidates share their skills, experience and work preferences. Employers describe what they need."],["2. Discover a fit","Employers can discover suitable, privacy-aware candidate profiles using supported search filters."],["3. Show interest","An employer can express interest in a candidate. The candidate is told clearly what this means."],["4. Match and connect","When interest is accepted, KAAM creates a match and enables the next step under the platform's access rules."]];return <><Header/><main><PageHero eyebrow="How KAAM works" title="A simpler path from profile to opportunity.">KAAM is not a traditional job board. A connection starts only when it makes sense for both sides.</PageHero><MarketingShell><div className="mx-auto max-w-3xl border-l-2 border-[#e53670] pl-6">{steps.map(([title,copy])=><section className="pb-9" key={title}><h2 className="text-xl font-bold text-[#201925]">{title}</h2><p className="mt-2 leading-7 text-[#66616f]">{copy}</p></section>)}</div></MarketingShell></main><Footer/></>}

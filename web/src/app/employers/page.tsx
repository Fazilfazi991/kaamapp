import type { Metadata } from "next";
import { Footer } from "@/components/layout/footer";
import { Header } from "@/components/layout/header";
import { MarketingShell, PageHero } from "@/components/marketing/page-hero";
import { ButtonLink } from "@/components/ui/button";
import { routes } from "@/config/routes";
export const metadata: Metadata = { title: "Hire workers", description: "Discover relevant workers with KAAM's skills-first mutual-interest hiring workflow." };
export default function EmployersPage() { return <><Header/><main><PageHero eyebrow="For employers" title="Find workers who fit the role—and want to hear from you.">KAAM makes it easier to discover relevant skills, express interest and connect after a mutual match.</PageHero><MarketingShell><ol className="grid gap-4 md:grid-cols-5">{["Set up your company", "Define hiring needs", "Search candidates", "Show interest", "Connect after a match"].map((step,index)=><li className="rounded-2xl border border-[#eadde3] p-5" key={step}><span className="text-sm font-bold text-[#bc1f55]">0{index+1}</span><p className="mt-3 font-bold text-[#201925]">{step}</p></li>)}</ol><div className="mt-10 grid gap-6 rounded-2xl bg-[#f8f5f7] p-7 md:grid-cols-2"><div><h2 className="text-2xl font-bold text-[#201925]">Relevant profiles, privacy-conscious hiring.</h2><p className="mt-3 leading-7 text-[#66616f]">Search supported filters such as skills, experience, location and work preferences. KAAM keeps private candidate information protected until business rules permit sharing.</p></div><div className="flex items-center"><ButtonLink href={`${routes.register}?role=employer`}>Start hiring</ButtonLink></div></div></MarketingShell></main><Footer/></> }

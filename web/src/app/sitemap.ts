import type { MetadataRoute } from "next";
export default function sitemap(): MetadataRoute.Sitemap { const base = process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.kaamcareer.com"; return ["", "/candidates", "/employers", "/how-it-works", "/about", "/contact", "/privacy", "/terms", "/account-deletion"].map((path) => ({ url: `${base}${path}`, lastModified: new Date(), changeFrequency: "monthly", priority: path === "" ? 1 : 0.7 })); }
